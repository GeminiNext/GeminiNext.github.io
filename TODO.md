# gemini4.cn 项目待办清单 (TODO)

为了完成整套自动化系统的闭环，请协助完成以下事项：

## 1. 自动化发布通知配置 (高优先级)
- [ ] **获取 Bark Key**：在手机或 Mac 上下载 Bark App，获取你的专属 Key。
- [ ] **设置 GitHub Secrets**：
    - 前往仓库 `Settings` -> `Secrets and variables` -> `Actions`。
    - 点击 `New repository secret`。
    - Name 填入 `BARK_KEY`，Value 填入你的 Key。
- [ ] **测试通知**：运行一次发布，观察手机/电脑是否收到推送。

## 2. 发布流程验证
- [ ] **全流程测试**：执行一次 `npm run release`，验证从本地提交到云端部署的完整链路。
- [ ] **分支检查**：发布完成后，确认本地 `dev` 分支与远程 `main` 分支状态一致。

## 3. 架构与内容持续优化 (可选)
- [ ] **图片资源迁移**：将旧的硬编码图片逐步迁移到 `public/blog_images/` 目录下统一管理。
- [ ] **SEO 监控**：部署成功后，检查 `gemini4.cn` 的搜索表现和元数据渲染。
- [ ] **脚本进阶**：根据使用习惯，决定是否需要重新引入“发布前自动关闭本地端口”或“本地静默等待”等功能。

---
*保持专注，持续进化。*
