# gemini4.cn 自动化发布工作流指南

本文档介绍本项目基于 GitHub CLI 和 GitHub Actions 搭建的“一键发布”系统。

## 1. 核心设计思路
项目采用 **双分支模型**：
- **`dev` 分支**：日常开发、博文撰写、功能调试的分支。
- **`main` 分支**：生产分支，任何同步到此分支的代码都会触发 GitHub Actions 自动构建并部署到 GitHub Pages。

通过自动化脚本，我们将“提交 -> 推送 -> 合并 -> 部署”这四个手动步骤压缩为一条指令。

## 2. 前置要求
在使用自动化发布之前，请确保：
1.  **安装 GitHub CLI**: `brew install gh`
2.  **完成登录授权**: `gh auth login` (已完成)
3.  **分支状态**: 确保你在 `dev` 分支上进行开发。

## 3. 日常发布指令

当你准备好发布新内容时，在项目根目录运行：

```bash
npm run release
```

### 脚本执行逻辑：
1.  **分支校验**：检查是否在 `dev` 分支，防止误操作。
2.  **交互提交**：提示输入 Commit Message 并自动执行 `git add .` 和 `git commit`。
3.  **代码同步**：将本地 `dev` 推送到远程仓库。
4.  **云端合并**：通过 `gh` 指令自动创建 Pull Request 并以管理员身份强制合并到 `main`。
5.  **自动部署**：GitHub Actions 检测到 `main` 的变动，开始执行打包部署任务。

## 4. 进度查看
发布成功后，终端会打印出 Actions 的进度链接。你可以点击查看实时构建状态：
`https://github.com/GeminiNext/GeminiNext/actions`

## 5. 故障排除
- **合并失败**：如果 `main` 分支领先于 `dev` 分支较多，可能存在冲突。请先手动执行 `git pull origin main` 将更改合并到 `dev`。
- **权限问题**：如果 `gh` 提示权限不足，请尝试重新运行 `gh auth refresh -s repo` 以获取完整仓库权限。

---
*Generated by Antigravity AI Guide*
